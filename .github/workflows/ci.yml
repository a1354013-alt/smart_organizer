name: CI

on:
  push:
  pull_request:

jobs:
  dependency-lock-determinism:
    name: Dependency Lock Determinism
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            requirements.lock.txt
            requirements-dev.lock.txt

      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.lock.txt

      - name: Python runtime preflight
        run: python -c "from runtime_preflight import require_supported_python; require_supported_python()"

      - name: Static lock validation
        run: python scripts/validate_dependency_locks.py --mode static

      - name: Seeded no-upgrade regeneration
        run: python scripts/validate_dependency_locks.py --mode regenerate

      - name: Repository unchanged check
        run: git diff --exit-code

      - name: Workspace clean check
        run: python scripts/check_workspace_clean.py --project-root .

  test:
    name: Tests (${{ matrix.os }}, Python ${{ matrix.python-version }})
    needs: dependency-lock-determinism
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            requirements.lock.txt
            requirements-dev.lock.txt

      - name: Install ffmpeg
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.lock.txt

      - name: Python runtime preflight
        run: python -c "from runtime_preflight import require_supported_python; require_supported_python()"

      - name: Clean release output
        shell: bash
        run: rm -rf release_ci coverage.xml .coverage .coverage.*

      - name: Compile Python files
        run: python scripts/safe_compileall.py -q .

      - name: Validate dependency locks
        run: python scripts/validate_dependency_locks.py --mode static

      - name: Ruff
        run: python -m ruff check --no-cache .

      - name: Mypy
        run: python -m mypy --cache-dir=/dev/null

      - name: Pytest ResourceWarning gate
        run: |
          python -W error::ResourceWarning -m pytest -q tests/test_storage_db_schema.py tests/test_runtime_config.py tests/test_storage.py tests/test_app_bootstrap.py

      - name: Pytest with coverage
        run: python -m pytest -q --cov=. --cov-branch --cov-report=term-missing --cov-report=xml

      - name: Upload coverage XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.os }}-py${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: ignore

      - name: Dependency audit
        run: python -m pip_audit -r requirements.lock.txt

      - name: Build release zip
        run: python scripts/create_release_zip.py --output-dir release_ci --zip-name smart_organizer-release-validation.zip

      - name: Verify release zip
        run: python scripts/verify_release_zip.py release_ci/smart_organizer-release-validation.zip

      - name: Workspace clean check
        run: python scripts/check_workspace_clean.py --project-root .

      - name: Validate release source command plan
        run: |
          python scripts/validate_release_source.py --dry-run

      - name: Validate release source execution
        run: |
          python scripts/validate_release_source.py --timeout-tail-lines 20

      - name: PowerShell syntax smoke
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $null = [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw create_release_zip.ps1), [ref]$null)

      - name: Cleanup release output
        if: always()
        shell: bash
        run: rm -rf release_ci coverage.xml .coverage .coverage.*
